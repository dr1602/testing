import { describe, expect, it } from 'vitest';

describe('utils', () => {
  it('checks that 1 equals 1', () => {
    expect(1).toEqual(1);
  });
  it('checks that 2 equals 2', () => {
    expect(2).toEqual(2);
  });
});
